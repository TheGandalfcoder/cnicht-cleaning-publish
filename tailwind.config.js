module.exports = {
  darkMode: 'class', // Enables manual dark mode via 'class'
  content: ['./**/*.html'], // Adjust this to match your project's HTML or component file paths
  theme: {
    extend: {},
  },
  plugins: [],
}; 